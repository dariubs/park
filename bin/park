#!/usr/bin/env ruby
# -*- coding: UTF-8 -*-

require 'thor'
require 'park'

class Commands < Thor
  include Park

  desc "current", "Show current config"
  def current
    current_config
  end

  desc "init", "List git accounts"
  def init
    init_config
  end

end

Commands.start(ARGV)
